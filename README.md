# Controle de Temperatura – Baú Frigorífico (Mock)

Projeto Vite + React + TypeScript + Tailwind + Recharts.

## Rodar localmente
```bash
npm install
npm run dev
```

## Conectar ao Traccar
Substitua os dados *mock* por chamadas à API do seu servidor (ex.: `/api/positions`, `/api/events`).